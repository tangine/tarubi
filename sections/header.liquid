<section class="header flex-row items-center container-fluid" data-section-id="{{ section.id }}" style="height: 48px;">
  <div class="nav flex-row items-center gap-10">
    {% for link in linklists[section.settings.menu].links %}
      <a class="nav-item" href="{{ link.url }}">{{ link.title }}</a>
    {% endfor %}
  </div>
  {% comment %} <div>
    {% for country in localization.available_countries %}
      <div>
        <span>{{ country.iso_code }}</span>
        <span>{{ country.name }}</span>
        <span>{{ country.currency.iso_code }}</span>
      </div>
    {% endfor %}
  </div> {% endcomment %}
</section>

{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "main-menu"
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "Header"
    }
  ]
}
{% endschema %}