<section class="product-information">
  {{ product.title }}
  {% for variant in product.variants %}
    {{ variant.title }}
  {% endfor %}
</section>

{% schema %}
{
  "name": "Product Information",
  "settings": [],
  "blocks": [],
  "presets": [
    {
      "name": "Product Information"
    }
  ]
}
{% endschema %}