<section id="{{ section.id }}" class="blog">
  <h1>{{ blog.title }}</h1>
  {% paginate blog.articels by 6 %}

  {% endpaginate %}
</section>

{% schema %}
{
  "name": "t:names.blog",
  "settings": [
    {
      "type": "range",
      "id": "page_size",
      "label": "page_size"
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "t:names.blog"
    }
  ]
}
{% endschema %}